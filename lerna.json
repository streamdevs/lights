{
  "packages": ["services/*", "packages/*"],
  "version": "3.0.0",
  "npmClient": "yarn",
  "useWorkspaces": true,
  "command": {
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md"],
      "registry": "https://npm.pkg.github.com/"
    },
    "version": {
      "allowBranch": "master"
    }
  }
}
